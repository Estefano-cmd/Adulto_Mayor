import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'adulto_mayor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
